# Models

This directory used to host bundled face recognition `.tflite` models.

The current app build no longer ships local face recognition or people clustering.
Group photo detection is still available through the Android face detection pipeline,
so this directory is intentionally kept free of large recognition assets.
